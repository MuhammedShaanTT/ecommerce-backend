server:
  port: 4000

spring:
  profiles:
    active: dev

  datasource:
    url: jdbc:mysql://localhost:3306/multi_vendor_ecommerce
    username: root
    password: your_password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: t1swa.official@gmail.com
    password: Shaantt@1407
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enabled: true
            required: true

jwt:
  password: ${EMAIL_APP_PASSWORD:your_app_password_here}
  expiration: 86400000

tiswa:
  admin-email: t1swa.official@gmail.com

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      show-details: always
  metrics:
    export:
      prometheus:
        enabled: true
